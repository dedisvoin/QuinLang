const arr1 = [0..10..2]
const arr2 = [0..30..3]

var arr = [arr1+arr2, true, false, 'What is it?']


var var_type
var elem_count

var_type = type(arr[0])
elem_count = len(arr)

print(
    arr1,
    arr2,
    arr,
    var_type,
    elem_count,
    type(arr[-1])
)